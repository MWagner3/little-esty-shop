<%= form_with model: @merchant, local: true do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.submit 'Submit'%>
<% end %>

 <%= render partial: 'form', locals{model: @merchant} %>
